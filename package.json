{
  "name": "todo-api-typescript-tests",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'add eslint later'",
    "test": "vitest run",
    "build": "tsc",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.9",
    "tsx": "^4.15.7"
  }
}
